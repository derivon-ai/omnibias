# Sheaf-atlas net (09-09)

Partition cells plus jet-valued transition maps. Training includes the
cocycle residual: chart `i -> j -> k` equals `i -> k` to order `N`.
Chart maps use founding bias collapse (`delta -> 0`) for `sigma^(n)`.
Partition gates may harden as `beta -> inf` (temperature collapse,
feasibility). Do not conflate the two.

Status is **gated**, not shipped. Not a rewrite of metrics-only
`AtlasSpec` blending. Not a sheaf-cohomology theorem. Not P vs NP on
arrangements. Not CCF stretch. Empty overlaps make the cocycle
vacuous.

Homes: `omnibias.geometry.atlas.cocycle`,
`omnibias.geometry.atlas.{torch,jax}`.

::: omnibias.geometry.atlas.cocycle
    options:
      show_root_heading: false
      heading_level: 3
