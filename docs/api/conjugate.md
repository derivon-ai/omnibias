# Conjugate Hilbert tower (01-12)

Line Hilbert only. Commutation with `d/dx` needs decay. G5 is a
projection defect, **not** a clearing of `CCF_STRETCH_RESIDUAL_GATE`.
The n-th Hardy derivatives on the line are interval-equal to the
existing n=1 case; Hilbert is a permutation of the activation
dictionary.

G1–G4 are CI-gated. G5 is a campaign artifact, **not** in CI
`all_passed`. Status is **gated**, not shipped. See theory spec 01-12.

## Core algebra

::: omnibias.core.conjugate
    options:
      show_root_heading: false
      heading_level: 3
