/-
The omnibias derivative tower, as Mathlib theorems with no `admit`.

Polynomial recurrences (`Tower.Poly`), first-derivative Riccati identities
(`Tower.Riccati`), and the `iteratedDeriv` link (`Tower.Link`). These are
pointwise `C^∞` / algebraic identities. They do not state a finite-difference
collapse, a continuum PDE claim, or any asymptotic.
-/

import OmnibiasAnalytic.Tower.Poly
import OmnibiasAnalytic.Tower.Riccati
import OmnibiasAnalytic.Tower.Link
